require "./host.c";

extern memory :: Unit -> Ptr;

main :: Unit -> Int32 := \() {
    base := memory();
    slot := base + 1u64;
    Int64.store(slot, 41i64);
    byte := slot + 8u64;
    UInt8.store(byte, 1u8);
    Int32(Int64.load(slot) + Int64(UInt8.load(byte)) - 42i64);
};
